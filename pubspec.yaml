name: force_directed_graphview
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ^3.0.0
  flutter: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # The following packages are direct dependencies of Flutter, 
  # so we don't need to specify version
  meta: any
  vector_math: any
  collection: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^5.0.0
